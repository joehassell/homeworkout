{
  "name": "simpleworkoutgen",
  "version": "1.0.0",
  "private": true,
  "description": "SimpleWorkoutGen — home workout builder, web + iOS native shell via Capacitor",
  "scripts": {
    "build:web": "rm -rf www && mkdir -p www && cp index.html www/ && cp -r js www/",
    "build": "npm run build:web",
    "sync:ios": "npm run build && npx cap sync ios",
    "open:ios": "npx cap open ios",
    "serve": "python3 -m http.server 8080"
  },
  "author": "Joe Hassell",
  "license": "UNLICENSED",
  "devDependencies": {
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.3.1",
    "@capacitor/keyboard": "^8.0.3",
    "@capacitor/status-bar": "^8.0.2"
  }
}
